define Xavier as agent {
	use makerInit as init.
	use makerEvents as event.
	use makerMachine as main.
}
define Logan as agent {
	use grinderInit as init.
	use grinderEvents as event.
	use grinderMachine as main.
}
launchpolicy {
	launch Xavier.
	launch Logan.
}